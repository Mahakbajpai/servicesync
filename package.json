{
  "name": "servicesync-monorepo",
  "version": "1.0.0",
  "description": "ServiceSync - Real-Time Service Booking Platform & Dashboard",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix blockchain",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "blockchain": "npm run node --prefix blockchain",
    "deploy:contract": "npm run deploy:local --prefix blockchain",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "dev:full": "concurrently \"npm run blockchain\" \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "Mahak Bajpai",
  "license": "ISC"
}
