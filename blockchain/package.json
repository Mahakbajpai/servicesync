{
  "name": "servicesync-blockchain",
  "version": "1.0.0",
  "description": "Hardhat Solidity setup for ServiceSync booking audit trail",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4",
    "servicesync-monorepo": "file:.."
  }
}
